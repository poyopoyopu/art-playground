# AGENTS.md — Art Playground 作業ルール

このリポジトリで作業する前に読んでください。しゅん(おっちゃん)とクロード(クロちゃん)が運用しているジェネラティブアートのプレイグラウンドです。

---

## 0. 一番大事なこと

- **削除しない。** ファイルもコードブロックも、明示的に頼まれない限り消さない。不要に見えても、まずコメントで指摘する。
- **`ap-kit.js` は編集しない。** v32〜v48以降、10作品以上がこのファイルに依存しています。直したい点があっても、コードは変えずコメントで指摘するだけに留めてください。
- **`index.html` は編集しない。** ギャラリーの入口で、他の人(クロード)も同時に触っています。ここが壊れるとサイト全体が死にます。
- **作業前に、そのファイルを一度GitHubから取得し直してから編集する。** 別の経路(クロードや手動アップロード)でも同じリポジトリが更新されるため、古いローカルコピーをベースに書き戻すと、他の変更を消してしまいます。

---

## 1. 何を作るか — 「世界のルールを変える」という基準

ART PLAYGROUNDは単なるジェネラティブアートのギャラリーではなく、**「触ることで作品世界のルールが変わる」**ことを重視したプロジェクトです。合言葉は「TOUCH THE WORLD · CHANGE ITS LAW」。

コードが動くかどうかより、次を評価基準にしてください。

- 触ったことで作品世界のルールが変わるか
- もう一度触りたくなる理由が生まれるか
- 触る前と後で、作品そのものの意味や状態が変化するか
- 単なるエフェクトではなく、作品として成立しているか

### 基本ルール

1. コンセプトをエフェクトより先に置く
2. 各作品に明確な「世界のルール」を1つ持たせる
3. tap / drag / hold / release などの操作に意味を持たせる
4. 操作によって状態・秩序・構造そのものが変化する
5. 「押した場所に光が出る」だけの作品は避ける
6. 美しさだけでなく、不思議さ・違和感・発見を作る
7. フラクタル、自律的な構造、自己相似なども積極的に使う
8. 技術デモではなく「作品」を作る
9. UIを増やしすぎない
10. 最終判断は「おっ」と思えるかどうか

### 陥りやすい失敗

「触っても点が増えるだけ」「粒子・光・線・波のどれかを変えただけ」の作品が量産され、見た目は違っても体験がほぼ同じになる問題が過去に起きました。**エフェクトを変えるのではなく、世界の法則を変えること**を最優先にしてください。例:

- 触れた場所だけ世界の秩序が崩れる
- 触れるほど物質の法則が変わる
- 一度壊した秩序は元に戻らない
- 触った場所が新しい法則の中心になる

このどれか(または別のオリジナルな法則)を、コードを書き始める前に1つ決めてください。既存作と比べて「体験の構造が本当に違うか」を自分で問い直し、似ているなら作り直すか別の方向にしてください。

---

## 2. ファイルの置き場所

| 状態 | 置き場所 | 命名 |
|---|---|---|
| 試作・アイデア段階 | `prototypes/` 直下 | 自由(`06-vein.html` のような連番でも、テーマ名でも可) |
| 採用が決まった作品 | リポジトリのルート直下 | `art-vNN-name.html`(NNは次の空き番号。しゅんかクロードが採番) |

prototypes/ 内の作品にコントロール類(時計・GIF等)を付けるのは歓迎ですが、**ルートへの昇格・ファイルのコピー・ギャラリー(index.html)への追加は自分ではやらない。** しゅんかクロードに任せてください。

---

## 3. 共有コントロールキット(`ap-kit.js`)の使い方

時計・フルスクリーン・GIF書き出し・共有ボタンは、作品ごとに実装しないでください。全部このキットが提供します。

### 組み込み方

**手順1** — `<head>` に追加(相対パスでリポジトリ直下を指す):
```html
<script src="ap-kit.js"></script>
```
`prototypes/` 内のファイルからは `<script src="../ap-kit.js"></script>` になります。

**手順2** — 初期化コードの最後に、これを1回だけ呼ぶ:
```js
APKit.init({
  canvas: /* canvas要素そのもの。p5.jsなら document.querySelector('canvas') */,
  gifName: 'このスケッチのファイル名-loop',
  clockFormat: 'spec',              // ← 必須。次の3章を参照
  dock: true,                       // ← 必須。下の表を参照
  autoHide: true,                   // ← 必須。下の表を参照
  fullscreenButton: false,          // ← 必須。下の表を参照
  loopFrame: function(phase){
    // phaseは0〜2πのループ位相。GIF書き出し中に呼ばれる。
    // 既存の描画関数を、時間変数の代わりにphaseを使って呼べるようにすること。
  },
  buttons: [
    {label:'✦', name:'mutate', onClick:function(){ /* 後述4章 */ }},
    {label:'↻', name:'reset',  onClick:function(){ /* 既存にリセット処理があれば */ }}
  ]
});
```

### 渡すオプションは省略しない

| オプション | 必須 | 忘れる/省略するとどうなるか |
|---|---|---|
| `canvas` | 必須 | GIF書き出しができない |
| `gifName` | 必須 | ダウンロードファイル名が `loop.gif` になる |
| `clockFormat: 'spec'` | **必須** | 時計が日本語形式(`9月11日（金）`)になる。仕様は3章の英語形式 |
| `dock: true` | **必須**(2026/09〜) | ボタンが右下にバラバラの丸ボタンで浮く。他作品と見た目が揃わない |
| `autoHide: true` | **必須**(2026/09〜) | 無操作時にボタン類が消えず常時画面に残る |
| `fullscreenButton: false` | **必須**(2026/09〜) | ⛶ボタンが余分に出る(ブラウザ/OS/ホーム画面アプリに既にあるので二重になる) |
| `loopFrame` | GIF機能を使うなら必須 | ◉ボタンが出ない、または壊れる |
| `buttons` | 空配列でも良いが、まず4章を読むこと | ✦や↻が出ない |

`dock` / `autoHide` / `fullscreenButton` の3つは元々このドキュメントに書かれていなかった不文律で、v35〜v58は守られていたがv59〜v63で崩れていた(2026/09、クロードとチャッピーの認識がズレて発覚)。しゅんの判断で**この3点セットを正式ルールとして確定**。以後の新作・改修は必ずこの3つを渡すこと。古い作品(v1〜v33, v59〜v63など)を見つけても、勝手に一括修正はせず、触る機会があったときにこの3つを追加する形で構わない。

**チェックリスト**: 実装が終わったら、この表の8項目を1つずつ「渡したか」確認してください。

---

## 4. 時計の仕様(絶対厳守)

- 時刻: `HH:MM:SS`。秒まで表示、大きく。
- 日付: `YYYY MM DD DDD`。月は数字(`SEP` などの英語月名は禁止)。曜日は3文字英語(`MON TUE WED THU FRI SAT SUN`)。
- 例: `21:02:14` の下に `2026 09 11 FRI`。
- ON/OFF可能。OFFで時刻・日付とも完全非表示。
- **これは `clockFormat: 'spec'` を渡すだけで自動的に実現されます。** 自分で日付のフォーマット処理を書かないこと。

---

## 5. ランダム生成ボタン(✦ mutate)は省略しない

「このスケッチには乱数の種になる変数がない」という理由でボタンを丸ごと省略しないでください。**何かしらランダム化できる要素を必ず見つけて実装すること。** 候補の考え方:

- 色相・パレットの基準値
- 対称数、渦の分割数
- 触った痕跡(marks/laws/voidsなど)を新しい位置・向きで生成し直す
- ノイズのseed値
- 力の強さ、減衰率、周期などのパラメータ

「本当に何もランダム化できる要素がない」場合だけ、ボタンを省略してよいですが、その場合は理由をコミットメッセージかコメントに一言書いてください。

---

## 6. 長押し操作を作るなら(iOSの落とし穴)

長押し(HOLD)を使う作品は、`ap-kit.js` を読み込んでいれば自動的に対策済みです。iOSのSafariは何も設定しないと、画面を長押しした時に「コピー / 調べる」のテキスト選択メニューを出そうとして、作品側のタッチ処理を邪魔します。キットが `-webkit-touch-callout:none` などをグローバルに注入して防いでいるので、**自分でこの対策を書く必要はありません**。ap-kit.jsを使わない単発のスケッチを作る場合だけ、`<style>` に以下を入れてください。

```css
html,body,canvas{-webkit-touch-callout:none;-webkit-user-select:none;user-select:none}
```

---

## 7. タッチ・クリックの実装で絶対に守ること

p5.jsやCanvasのタッチ処理で `return false`(= `preventDefault()`)を使う場合、**ハンドラの先頭で必ずこう書く**:

```js
if (window.APKit && APKit.isUI(x, y)) return true;
```

(`x, y` はそのイベントの画面座標。p5なら `mouseX, mouseY` や `touches[0].x, touches[0].y`)

これを忘れると、キットのボタン(◉のGIF書き出し選択肢を含む)がタップに反応しなくなります。**これは過去に2回、別々の作品で実際に起きたバグです。** ボタンを追加したら、必ず実機かシミュレータで✦・↻・◉・🕐・⛶の全部を一度タップして確認してください。

---

## 8. GIFループについて

`loopFrame(phase)` は、`phase` が `0` のときと `2π` のときに**まったく同じ画になる**のが理想です(継ぎ目のないループ)。

- 時間に `sin(time * k)` のような項がある場合、`time` の代わりに `phase` を使い、`k` を「3秒間で整数回転する値」に丸めると継ぎ目が消えます。
- 完璧に閉じさせるのが難しい作品(蓄積型の軌跡など)は、無理をしなくて構いません。その場合は継ぎ目が多少出ることをコミットメッセージに書いておいてください。仕上げが必要な作品は、しゅんかクロードが引き取ります。
- **do not** `time` をそのまま `loopFrame` の外から進め続けないこと。GIF書き出し中は静止画がずれた状態で量産されます。

---

## 9. 命名・クレジット表記

- 作品タイトルは英語で短く、大文字(`VEIN`, `VOID FIELD` のように)。
- ファイルの冒頭に、自分が作った/改変した旨をコメントで残しておくと後で分かりやすいです:
  ```html
  <!-- VEIN — ChatGPT作。ap-kit.js統合: ChatGPT -->
  ```

---

## 10. 好みの方向性 — フラクタル・自己相似

しゅんはフラクタル構造(自己相似な入れ子)が好きです。新作を作るとき、以下を優先的に検討してください。

- **IFS(反復関数系)** — シダの葉やマンダラのように、決まった変換を繰り返して自己相似を作る手法。触るとその変換のパラメータが変わり、全体の形が連続的に変形すると理想的。
- **再帰的な折り畳み** — 座標を繰り返し折り返す(fold)処理を、浅い回数(3〜4回)ではなく深く(6回以上)・不規則にかけて、細部が全体構造を繰り返す絵を作る。
- **階層的なスケール** — 大きい構造の中に、縮小した相似形が入れ子になっている。粒子系なら「大きい渦の中に小さい渦、その中にさらに小さい渦」のように力場を複数スケールで重ねる。

**避けてほしいもの**: 単純に模様を細かくしただけ(自己相似になっていない高周波ノイズ)、フラクタルと呼びながら実際は1段階の繰り返ししかないもの。「同じ変換を最低3階層は繰り返しているか」を自己チェックしてから提出してください。

---

## 11. 困ったとき

- `ap-kit.js` の挙動がおかしい、仕様が足りない → **自分で直さず**、コメントかコミットメッセージで具体的に指摘する。
- どのファイルを触ればいいか分からない → prototypes/ 以外は基本的に触らない。
- 大きな変更をしたい(index.htmlの構成、ap-kit.js自体の仕様変更など) → しゅんに確認してから。

## 12. worker.js でHTMLを書き換える発想は基本NG

`wrangler.toml` の `run_worker_first` は `/api/*` にしか設定してない。つまり `/api/*` 以外(HTMLファイル全部)へのリクエストは **`worker.js` の `fetch()` を素通りして静的アセットが直接返る**。worker.js側で正規表現やDOM操作でHTMLを書き換えて仕様を揃えようとしても、そのページが `/api/*` でなければ一生実行されない。

実際にこれで時計の日付フォーマットが直らないというバグが起きた(2026年9月)。直し方は「Workerで書き換える」ではなく、**該当ファイル本体を直接編集する**こと。
